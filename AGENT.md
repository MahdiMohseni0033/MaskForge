# MedSAM workspace guidance

The workspace contains three independent projects under `medsam1/`, `medsam3/`, and `sam3/`. Read the selected project's `AGENT.md` and `tracker.md` before changing it. Do not mix their environments, packages, upstream repositories, or checkpoints.

Keep both applications as technical research prototypes and use only de-identified images. Run validation from the relevant project directory.
